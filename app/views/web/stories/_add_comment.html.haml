.span12
  = simple_form_for(comment, url: api_v1_story_comments_path(story), remote: true) do |f|
    = f.input :body, as: :text, input_html: {rows: 4}, label: false
    = f.input :parent_id, as: :hidden
    = f.button :submit, t('.add_comment_btn')