.form-page-bg
  .form-page
    .row.span12
      %h3= t('.sign_up_title')
    .row.span12
      = simple_form_for @user do |f|
        = f.input :email
        = f.input :name
        = f.input :password
        = f.input :password_confirmation

        = f.button :submit, t('.new_user_btn')
